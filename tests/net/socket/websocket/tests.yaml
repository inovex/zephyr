common:
  depends_on: netif
tests:
  net.socket.websocket:
    min_ram: 21
    tags:
      - net
      - websocket
  net.socket.websocket.dedicated_heap:
    min_ram: 21
    tags:
      - net
      - websocket
    extra_configs:
      - CONFIG_WEBSOCKET_DEDICATED_HEAP=y
